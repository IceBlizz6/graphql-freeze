{
	"name": "graphql-freeze",
	"version": "0.1.0",
	"type": "module",
	"scripts": {
		"clean": "rimraf dist",
		"build": "pnpm run clean && tsc && ncp package.json dist/package.json"
	},
	"devDependencies": {
		"rimraf": "6.0.1",
		"typescript": "5.6.3",
		"ncp": "2.0.0"
	}
}
